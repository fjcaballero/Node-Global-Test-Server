{
  "name": "server",
  "version": "1.0.0",
  "description": "node server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "sass --watch public/sass:public/css &",
    "start": "nodemon app.js",
    "w-start":"START /B sass --watch public/sass:public/css & nodemon app.js"
  },
  "author": "Javier Caballero",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0",
    "nodemon": "^1.11.0",
    "webcomponents.js": "^0.7.23"
  }
}
